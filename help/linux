To remove a package:
sudo apt --purge remove 'packagename'

Intall a package:
sudo dpkg -i *.deb

Extract a tar file:
tar -xf *.tar|tar.gz

Extract a rar file:
unrar x file.rar file_destine

update npm:
sudo npm install -g npm
